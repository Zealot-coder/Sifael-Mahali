# Shared Types

This directory is reserved for cross-layer TypeScript types:
- Domain models
- API contracts
- Owner/public DTOs
- Supabase row mappings

Current:
- `supabase.ts` (generated/managed via `npm run supabase:types`)
- `index.ts` type exports
