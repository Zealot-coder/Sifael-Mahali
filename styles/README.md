# Styles Workspace

Use this directory for shared style assets:
- design tokens
- motion tokens
- theme layers

Current global stylesheet remains at `app/globals.css` and can be migrated here incrementally.
